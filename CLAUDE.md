@AGENTS.md

# 프로젝트 규칙 — 대주단 제출용 사업계획서 앱

시행사가 대주단에 낼 사업계획서를 만드는 앱이다. 특정 사업장을 넣지 않고, 입력한 값으로 사업계획서가 나온다.

## 진행 상황

- [x] 입력 폼 · 임시 값 버튼 3개
- [x] 계산 (분양수입 · 기말현금 · DSCR · LTV · 한계선) + 검산 테스트
- [x] 2장 시장 분석 — 사업대상지 기준 공공데이터 연결 · 네 표 · 결론 옆 시장 대비 여유
- [x] 차트: 시장 대비 여유 · 사업수지 폭포
- [ ] 차트: 분양권 웃돈 분포 · 월별 거래 건수 · 청약 경쟁률 · 상환 순서 계단 · 분양률×기말현금 곡선 · 민감도 히트맵
- [ ] 사업계획서 목차 1 · 4 · 5 · 6 · 7 · 민감도 표
- [ ] GitHub CLI(`gh`)로 저장소 생성·푸시 → Vercel 배포

사용자가 요청한 범위까지만 만들고 멈춘다. 다음 단계를 미리 만들지 않는다.

## 기술 제약

- 설치된 Next.js(16.3.5) 문서(`node_modules/next/dist/docs/`) 기준 · App Router · TypeScript. `src/` 없이 루트에 `app/` · `lib/` · `components/`.
- 계산은 `lib/calc.ts` 순수 함수. React·Next·브라우저 API 의존 금지. **모든 함수에 한국어 주석.**
- 시장 분석 표·응답 정리는 `lib/market.ts` 순수 함수. 네트워크·인증키는 `lib/server/`에서만.
- 입력 · 결과 화면은 `components/PlanApp.tsx` (시장 분석 `components/MarketAnalysis.tsx`, 호출 훅 `components/useMarketData.ts`).
- 임시 값은 `lib/presets.ts`.
- 검산은 `lib/*.test.ts` — `node --test`(node:test · node:assert). **새 테스트 도구 설치 금지.**
- `lib/` 안에서 서로 import할 때와 테스트에서는 `.ts` 확장자를 붙인다. 타입만 가져올 때는 `import type`(Node 타입 제거 실행 때문에 필수).

## 명령어

- `npm run dev` — 개발 서버 (http://localhost:3000)
- `npm test` — `node --test "lib/**/*.test.ts"`
- `npm run typecheck` — `next typegen && tsc --noEmit`
- `npm run lint` / `npm run build`

작업을 마치기 전에 `npm test`, `npm run typecheck`, `npm run lint`를 모두 통과시킨다. 실패하면 그대로 보고한다.

## 입력 — 단위 억원, 평당가만 만원

- 사업명 · 사업대상지(시도 + 시군구, 둘 다 전체 이름)
- 주택형 표(이름 · 전용㎡ · 공급평 · 세대수) → 공급면적 합계 · 세대수 계산
- 평당가(공급기준) · 상가 · 기타유입 · 고정비 · 본PF · 본PF 금융비용 · 후순위
- 처음엔 빈 폼. 입력하지 않은 숫자는 `null`(0과 구분).

## 계산 — 분양률 r, 평당가 p

- 분양수입 = (공급면적 × p ÷ 10,000 + 상가) × r
- 기말현금 = 분양수입 × 0.992 − 세대수 × r × 0.03 + 기타유입 − 고정비
- 만기 누적 DSCR = 1 + (기말현금 + 후순위) ÷ (본PF + 본PF 금융비용)
- LTV = 본PF ÷ 분양수입(r = 1)
- 한계선(이분법): 기말현금 0 / −후순위 — 분양률 기준 · 평당가 기준
- 상환 가능 = 기말현금 ≥ −후순위

## 검산 테스트

- 샘플 A(해운대 그린테라스)가 이 값이어야 통과: 기말현금 +380.3억 · DSCR 1.27 · LTV 60.2% · 기말현금 0 한계 90.5% · 2,183만원 · 상환 한계 80.6% · 1,934만원
- 기대값은 코드를 돌려 복사하지 말고 손계산으로 적고, 테스트 이름이나 주석에 계산식을 남긴다.
- 계산 함수를 추가·변경하면 같은 작업에서 테스트를 추가·수정한다.
- 공공데이터 응답 해석 테스트는 `api_docs` 기술문서의 응답 예제를 그대로 쓴다. 표 계산 테스트의 단지는 가상 자료만.

## 공공데이터 (2장 시장 분석)

- 키는 `.env.local`의 `DATA_GO_KR_KEY` — 서버 코드에서만 읽는다. `NEXT_PUBLIC_` 금지 · 키를 쓰는 파일은 `import "server-only"` · 키 값과 키가 든 요청 URL은 응답·로그·오류 메시지에 절대 출력하지 않는다.
- 공공데이터 호출은 하루 캐시, 법정동코드는 30일 캐시. (`unstable_cache`로 감싸 실패 응답은 캐시하지 않는다)
- 브라우저는 지역 이름(시도 시군구)만 보내고, 법정동코드는 서버가 확인한다.
- 빌드 때 API를 부르지 않는다 — 라우트는 모두 요청 시 실행. 키 없이도 `npm run build`가 통과해야 한다.
- 주소·파라미터·응답 필드는 `api_docs/` 기술문서에 있는 것만 쓴다.
- 라우트 (모두 `area=시도 시군구`)
  - `/api/region?q=` — 법정동코드 시군구 후보
  - `/api/market/presale?ym=` · `/api/market/rent?ym=` — 한 달치 (분양권은 해제 제외, 전월세는 기술문서에 해제여부 항목 없음)
  - `/api/market/subscription` — 공급위치 LIKE 시군구 · 최근 3년 · APT 분양 + 잔여세대
  - `/api/market/subscription/detail?id=&pblanc=` — 주택형별 분양가 · 경쟁률
- [사업계획서 만들기] → 지역 확인 → 동시 4개까지 호출, 진행 표시(분양권 n/12개월 · 전월세 n/12개월 · 청약홈 k/N곳), 끝난 표부터 렌더링, 실패 항목은 오류와 [다시 시도]. 실패가 남은 표는 부분 숫자를 보여주지 않는다.
- 네 표(`lib/market.ts`): 인근 신규 84㎡ 공급평당 vs 계획가 · 분양권 웃돈(입주권 제외) · 1·2순위 미달 · 잔여세대 · 84㎡ 신규 전세 ÷ 우리 84㎡ 세대당 분양가
- 결론 옆 시장 대비 여유 = (인근 신규 84㎡ 공급평당 중위 − 상환 한계 평당가) ÷ 시장 중위
- 표마다 지역 · 건수 · 제외 기준 · 조회일 · 출처.
- 판단 기준 (바꾸면 표 문구도 함께): 84㎡형 = 전용 80~85㎡ · 신규 전세 = 월세 0 · 건축 5년 이내 · 웃돈 매칭 = 단지명 포함 관계 + 전용 ±0.05㎡ · 분양가 = 주택형 공급금액(최고).

## 차트

- 차트 라이브러리 설치 금지 — SVG로 직접. `components/charts/`에 차트마다 컴포넌트 하나 (공통 틀 `ChartFrame`, 색 `chartTheme`).
- 숫자는 `lib/calc` · `lib/market` 결과만 — 차트에서 다시 계산 금지 (눈금·좌표 변환만).
- 차트 아래 제목 · 단위 · 건수 · 조회일 · 출처. 데이터가 없으면 빈 축 대신 "자료 없음".
- 색 고정: 계획가 강조색(파랑) · 시장 회색 · 가능 초록 · 불가 빨강.
- 차트마다 [표로 보기] 토글 · 모바일 세로 1열 · 인쇄 A4.
- 2장: 시장 대비 여유(점 분포 + 계획가 선 + 상환 한계선) · 분양권 웃돈 분포 막대(0 아래 빨강) · 월별 거래 건수(분양권·전월세 12개월) · 청약 경쟁률(단지·주택형별, 미달 빨강)
- 3~5장: 사업수지 폭포 · 상환 순서 계단(본PF → 후순위 → 시행사) · 분양률×기말현금 곡선(0선 · −후순위선 · 현재 위치 점) · 민감도 히트맵(가능 초록/불가 빨강 · 경계선)

## 사업계획서 목차

1 사업 개요 · 2 시장 분석 · 분양가 근거 · 3 사업수지 · 4 자금조달과 상환 · 5 민감도 · 6 리스크와 완화 방안 · 7 확인하지 못한 것 — "월별 자금수지 · IRR은 이 앱의 범위 밖" 명시

- 민감도: 행 = 분양률 75~100%(5%p), 열 = 계획 평당가 기준 −500~+100만원(100만원 간격), 셀 = 기말현금, 상환 가능 초록 / 불가 빨강. 표 아래 "평당 OOO만원 / 분양률 OO% 이상이면 상환 가능" 자동 문장.

## 표시 규칙

- 입력이 비어 있으면 해당 장은 "자료 없음". 공공데이터를 못 받으면 "자료 없음 — 공공데이터 확인 필요".
- 각 지표 옆에 계산에 쓴 수식 · 천단위 콤마 · 음수는 빨간색.
- 모바일에서 세로 1열 · 인쇄하면 A4 문서.

## 하지 말 것

- 근거 없는 숫자 채우기
- 실제 사업장 데이터 (프리셋·테스트에)
- 인증키 기록

## 작업 방식

- UI 문구와 사용자 대상 메시지는 한국어.
- 새 의존성은 꼭 필요할 때만 추가하고 이유를 알린다.
- git 커밋·푸시, GitHub 저장소 생성, Vercel 배포는 사용자가 요청할 때만.
